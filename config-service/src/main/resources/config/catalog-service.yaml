server:
  port: 0

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

logging:
  level:
    org:
      springframework: ERROR

spring:
  application:
    name: catalog-service

  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce_catalog?createDatabaseIfNotExist=True
    username: root
    password: ebuka1111
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
